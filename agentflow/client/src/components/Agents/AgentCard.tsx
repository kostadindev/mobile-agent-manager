// Agent rendering now handled inline in AgentList.tsx via Konsta ListItem
import type { Agent } from '../../types/agents';

export default function AgentCard({ agent: _agent }: { agent: Agent }) {
  return null;
}
