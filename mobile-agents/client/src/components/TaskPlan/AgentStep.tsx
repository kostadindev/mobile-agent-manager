// Agent steps now rendered via Konsta ListItem in TaskPlanCard.tsx
import type { TaskStep } from '../../types/tasks';

export default function AgentStep({ step: _step }: { step: TaskStep; index: number }) {
  return null;
}
